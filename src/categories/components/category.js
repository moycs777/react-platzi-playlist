import React from 'react';

function Category(props) {
    return(
        <div>
            {
                props.playlist.map( (item)=>{

                })
            }
        </div>
    )

}

export default Category;